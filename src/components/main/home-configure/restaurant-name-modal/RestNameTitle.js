import React from 'react'
import DialogTitle from '@material-ui/core/DialogTitle'

const RestNameTitle = ({title}) => {
    return (<DialogTitle>Editing {title}</DialogTitle>)
}

export default RestNameTitle

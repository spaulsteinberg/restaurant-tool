import React from 'react'

const OpenModalPrompt = ({handleShowModal}) => {
    return (
        <p onClick={handleShowModal} className='open-modal-prompt'>Edit my buttons here link to open modal</p>
    )
}

export default OpenModalPrompt
